# AGENTS.md

이 문서는 이 저장소에서 Codex가 작업할 때 반드시 따라야 할 운영 규칙과 설계 원칙을 정의한다.

## PR 및 브랜치 운영

- PR 제목에는 `[Codex]`를 붙이지 않는다.
- PR 제목은 변경 유형에 맞게 `feat.`, `fix.`, `docs.`, `refactor.`, `test.`, `chore.` 등으로 시작한다.
- PR은 draft로 만들지 않고 즉시 Ready 상태로 생성한다.
- PR merge 후에는 작업 브랜치를 삭제해 정리한다.
- `코드리뷰 검토`라고 요청받으면 열린 PR의 댓글과 대댓글을 모두 확인한다.
- 코드리뷰 검토 결과 이슈가 없으면 merge한다.
- 수정이 필요한 경우에는 수정 후 push하고, merge하지 않은 상태로 대기한다.
- 코드리뷰 시 리뷰어 의견이 실제 수정 필요인지 환각인지 검토하고, 각 comment에 수정 여부 또는 환각 여부를 답변한다.

## SOLID 설계 원칙

코드를 작성하거나 수정할 때는 아래 SOLID 원칙을 우선 적용한다.

### Single Responsibility Principle

- 하나의 타입, 함수, 모듈은 하나의 책임만 갖도록 설계한다.
- UI, 상태 관리, 데이터 변환, 네트워크 요청, 영속화 로직을 가능한 한 분리한다.
- 변경 이유가 다른 로직이 한 파일이나 한 객체에 섞이면 분리한다.

### Open/Closed Principle

- 기존 동작을 직접 수정하기보다 확장 가능한 구조를 우선 고려한다.
- 조건문이 계속 늘어나는 영역은 전략 객체, 프로토콜, enum 기반 분기, 팩토리 등으로 정리한다.
- 단, 과한 추상화는 피하고 실제 중복 또는 변경 가능성이 확인된 지점에만 적용한다.

### Liskov Substitution Principle

- 상위 타입을 사용하는 코드는 하위 타입이 대체되어도 깨지지 않아야 한다.
- 프로토콜이나 상속 구조를 만들 때 구현체별 예외 동작을 강요하지 않는다.
- optional 구현이나 fatal path가 필요한 추상화는 설계를 다시 검토한다.

### Interface Segregation Principle

- 클라이언트가 사용하지 않는 메서드에 의존하지 않도록 작은 인터페이스를 선호한다.
- 큰 프로토콜 하나보다 역할별로 나뉜 프로토콜을 우선한다.
- 테스트 더블을 만들기 어려운 인터페이스는 책임이 과도한지 확인한다.

### Dependency Inversion Principle

- 고수준 정책은 저수준 구현체에 직접 의존하지 않도록 한다.
- 외부 API, 저장소, 네트워크, 시간, 랜덤 값 등은 주입 가능한 의존성으로 다룬다.
- 테스트 가능한 구조를 위해 구체 타입 직접 생성은 경계 지점으로 모은다.

## 구현 기준

- 기존 코드 스타일과 아키텍처를 먼저 확인하고 그 흐름을 따른다.
- 변경 범위는 요청된 문제 해결에 필요한 만큼으로 제한한다.
- 사용자 변경사항을 임의로 되돌리지 않는다.
- 복잡한 로직을 추가할 때는 테스트 가능성을 함께 고려한다.
- 불필요한 대규모 리팩터링보다 작은 단위의 명확한 개선을 선호한다.

